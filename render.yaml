services:
  - type: web
    name: agropredict
    env: docker
    envVars:
      - key: SECRET_KEY
        value: crop_prediction_secure_secret_key_2026
      - key: DB_PATH
        value: /data/crop_app.db
    disk:
      name: agropredict-data
      mountPath: /data
      sizeGB: 1
